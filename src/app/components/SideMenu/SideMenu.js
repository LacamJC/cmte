import styles from '../../scss/header/header.module.css'

const SideMenu = () => {
    return(
        <nav className={`${styles.sideBar}`}>
            <h1>CMTE Hamilton</h1>
        </nav>
    )
}

export default SideMenu